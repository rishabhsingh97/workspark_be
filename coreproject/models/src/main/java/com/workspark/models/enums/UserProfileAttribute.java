package com.workspark.models.enums;

/**
 * These are the allowed attributes for the user profile page.
 */
public enum UserProfileAttribute {
    first_name,
    last_name,
    phone,
    department,
    image,
    designation,
    office_location
}
